---
status: draft
site: seocompare
type: technical_fix
target_query: (n/a - broken internal links fix)
target_url: https://seocompare.co.uk/alternatives/[slug]/
current_state: |
  FLEET_INBOX (2026-05-04, still open): "SC: ~10+ broken /compare/<X>-vs-<Y>/ paths cited
  from /alternatives/<X>/ — the comparison pages don't exist. Either build the pages or
  strip the links."
  
  Source: src/pages/alternatives/[slug].astro line 192 generates links like:
  /compare/${[target.slug, a.slug].sort().join('-vs-')}/
  
  The compare pages directory (src/pages/compare/) only contains:
  - [pair].astro (dynamic route)
  - index.astro
  
  The dynamic [pair].astro should generate all compare pages from data. Need to verify whether
  the data source that feeds [pair].astro covers all the pairs referenced from alternatives pages,
  or whether some pairs are orphaned (linked from alternatives but not in the compare data).
proposed_change: |
  Step 1: Audit the alternatives data (src/data/agencies.ts or wherever alternative slugs come from)
  to find all pairs generated by alternatives/[slug].astro.
  
  Step 2: Verify [pair].astro can generate pages for each pair (check the compare data source).
  
  Step 3: Either:
  (a) Add missing pairs to the compare data source so [pair].astro generates them, OR
  (b) Remove the broken compare links from [slug].astro for pairs that don't have compare data.
  
  Option (b) is safer if the compare data is curated and not all agency pairs make sense to compare.
  
  Build + verify 0 broken internal links with find-broken.cjs before pushing.
  
  Note: SC is on adam@muswellrose.com CF account. Commit to main (auto-deploys via CF Pages).
why: |
  Broken internal links from /alternatives/ pages harm crawl efficiency and user experience.
  The FLEET_INBOX flagged this on 2026-05-04 and it remains open. SC needs SC-specific
  Clarity tag (Adam action, noted separately) and this technical fix is fully in-session scope.
  Fixing broken links removes Googlebot/AI crawler dead-ends from a key commercial hub.
---
