---
status: draft
site: market-invoice
type: new_provider_entries
target_query: penny invoice finance | marketfinance invoice finance
target_url: /alternatives/penny/ and /alternatives/marketfinance/ (both currently 404)
current_state: |
  Per GSC (strategist analysis 25 May), "penny invoice finance" gets 20 impressions/wk
  with 0 clicks. Penny is not in providers.ts so no /providers/penny/ page exists and
  no /alternatives/penny/ page can be generated by [slug].astro. Same for MarketFinance
  (an established UK invoice finance provider, formerly MarketInvoice's namesake competitor —
  ironic naming overlap; MarketInvoice is the editorial site, MarketFinance is the lender).
proposed_change: |
  Add two new provider entries to ~/compare-invoice-finance/src/data/providers.ts:

  1. Penny — UK fintech invoice finance, founded ~2020, app-first onboarding,
     SME-focused (sub-£500k turnover). Need real Companies House number,
     real FCA register entry, real founded year before adding.

  2. MarketFinance — UK fintech (rebranded from MarketInvoice 2020),
     business loans + invoice finance, larger SME focus.
     Companies House: MarketInvoice Finance Limited 06560649 (verify).

  Both entries follow the existing Provider type schema in providers.ts.
  Both auto-generate /providers/<slug>/ + /alternatives/<slug>/ pages
  via the existing [slug].astro templates.

  After adding, also add entries to ALTERNATIVES dict in alternatives.ts:
    'penny': ['kriya', 'sonovate', 'iwoca', 'capitalise'],  // fintech-fintech
    'marketfinance': ['kriya', 'bibby-financial-services', 'sonovate', 'aldermore-invoice-finance'],
why: |
  GSC data confirms these are real branded queries hitting MI's impressions but
  bouncing to competitors because MI has no capture page. The /alternatives/<slug>/
  template-generated content is exactly the format that lifts position-3-and-4
  results into the top-2 for "alternatives to X" queries. This is a one-time data
  entry + verification task (Companies House lookups for both brands) that turns
  ~25-30 monthly impressions per brand into capturable traffic.

  Adam to verify: real Companies House numbers + founded years + FCA register
  status (or omit FCA field if uncertain — see "no fabricated regulatory data"
  rule in MI CLAUDE.md). The agent should NOT fabricate any registration data.
---
